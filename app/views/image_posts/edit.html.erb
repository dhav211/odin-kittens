<h2>Editing Post</h2>

<%= form_with model: @post, url: kitten_image_post_path do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title, value: @post.title %>
  <br>
  <%= form.label :body %>
  <%= form.text_area :body, value: @post.body %>
  <br>
  <%= form.submit %>
<% end %>